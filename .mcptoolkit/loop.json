{
  "_": "The project loop file (docs/guides/LOOPS.md). `checks` run after editing tools and once more after a unit session; `unit.start` runs before one. Written once by toolkitInit; yours. The assets check was dropped: mcp-server 0.75.0 refuses a check with no `after`, and this project runs with mcmod.assets.check=false.",
  "checks": []
}
